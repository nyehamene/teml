[[language]]
name = "grammar"
scope = "source.grammar"
injection-regex = "grammar"
file-types = ["grammar"]
comment-tokens = "//"
indent = { tab-width = 2, unit = "  " }
roots = ["token.grammar", "ast.grammar"]

[[grammar]]
name = "grammar"
source = { path = "$HOME/.local/tree-sitter/tree-sitter-grammar/" }
